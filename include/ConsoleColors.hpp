#pragma once

namespace axioo {
namespace console {
    // Standard console colors for text and background
    // Dark colors (0-7)
    const int BLACK = 0;        // Basic black
    const int BLUE = 1;         // Dark blue
    const int GREEN = 2;        // Dark green
    const int CYAN = 3;         // Dark cyan
    const int RED = 4;          // Dark red
    const int MAGENTA = 5;      // Dark magenta
    const int BROWN = 6;        // Brown/Dark yellow
    const int LIGHTGRAY = 7;    // Light gray

    // Bright colors (8-15)
    const int DARKGRAY = 8;     // Dark gray
    const int LIGHTBLUE = 9;    // Bright blue
    const int LIGHTGREEN = 10;  // Bright green
    const int LIGHTCYAN = 11;   // Bright cyan
    const int LIGHTRED = 12;    // Bright red
    const int LIGHTMAGENTA = 13;// Bright magenta
    const int YELLOW = 14;      // Bright yellow
    const int WHITE = 15;       // Bright white
}
} 